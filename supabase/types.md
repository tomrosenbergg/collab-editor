# Supabase Types

Generate types and replace the placeholder in `src/types/supabase.ts`:

```bash
supabase gen types typescript --project-id <PROJECT_ID> > src/types/supabase.ts
```

Then restart the dev server to ensure types are picked up.
